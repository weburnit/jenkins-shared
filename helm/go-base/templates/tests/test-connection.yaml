apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-base.fullname" . }}-test-connection"
  labels:
    {{- include "go-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
